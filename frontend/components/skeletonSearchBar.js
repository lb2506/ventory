import { Skeleton } from "@rneui/themed";

const SkeletonSearchBar = () => {
  return <Skeleton height={50} style={{ margin: 10, borderRadius: 0, width: 345 }} />;
};

export default SkeletonSearchBar;
